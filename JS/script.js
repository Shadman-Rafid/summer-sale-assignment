function spoonCard(){
    const spoon = getCardValue('spoon-price');  
    setTotalPrice(spoon);
    const spoonName = getCardName('card1-name');
    addToPurchaseList(spoonName);
}
function plateCard(){
    const plate = getCardValue('plate-price');  
    setTotalPrice(plate);
    const plateName = getCardName('card2-name');
    addToPurchaseList(plateName);
}
function cookerCard(){
    const cooker = getCardValue('cooker-price');
    setTotalPrice(cooker);
    const cookerName = getCardName('card3-name');
    addToPurchaseList(cookerName);
}
function blackCapCard(){
    const blackCap = getCardValue('cap-price');
    setTotalPrice(blackCap);
    const capName = getCardName('card4-name');
    addToPurchaseList(capName);
}
function jerseyCard(){
    const jersey = getCardValue('jersey-price');
    setTotalPrice(jersey);
    const jerseyName = getCardName('card5-name');
    addToPurchaseList(jerseyName);
}
function shoeCard(){
    const shoe = getCardValue('shoe-price');
    setTotalPrice(shoe);
    const shoeName = getCardName('card6-name');
    addToPurchaseList(shoeName);
}
function relaxChairCard(){
    const relaxChair = getCardValue('relaxChair-price');
    setTotalPrice(relaxChair);
    const chairName = getCardName('card7-name');
    addToPurchaseList(chairName);
}
function childPlayCard(){
    const childPlay = getCardValue('childPlay-price');
    setTotalPrice(childPlay);
    const childName = getCardName('card8-name');
    addToPurchaseList(childName);
}
function sofaCard(){
    const sofa = getCardValue('sofa-price');
    setTotalPrice(sofa);
    const sofaName = getCardName('card9-name');
    addToPurchaseList(sofaName);
}


